/*
How are funvtions implemented behind the scene?
How to handle function calls?
"Runtime Stack" (or "Call Stack")
-Keeps track of funcions currently running
-Stores the following information for each call:
  -Storage for parameters
  -Storage for local variables
  -"return address"(Instruction Pointer for code after the call 'IP')
  -Storage for return value.
  Look at the notes

  */


